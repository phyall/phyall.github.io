<nav class="dept-nav">

  <div class="dept-nav-container">

    <ul class="dept-nav-list">

      {% for item in site.data.navigation.main %}

        {% assign active = false %}

        {% if page.url == item.url %}
          {% assign active = true %}
        {% endif %}

        {% if item.children %}
          {% for child in item.children %}
            {% if page.url == child.url %}
              {% assign active = true %}
            {% endif %}
          {% endfor %}
        {% endif %}

        <li class="dept-nav-item{% if item.children %} has-dropdown{% endif %}">

          <a href="{{ item.url | default: '#' | relative_url }}"
             class="{% if active %}active{% endif %}">

            {{ item.title }}

          </a>

          {% if item.children %}

          <ul class="dropdown-menu">

            {% for child in item.children %}

            <li>

              <a href="{{ child.url | relative_url }}">

                {{ child.title }}

              </a>

            </li>

            {% endfor %}

          </ul>

          {% endif %}

        </li>

      {% endfor %}

    </ul>

  </div>

</nav>
